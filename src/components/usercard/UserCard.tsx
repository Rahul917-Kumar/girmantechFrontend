import { searchResultInterface } from "@/types"


function UserCard({ id, first_name, last_name, contact_number, city}: searchResultInterface){
    return (
        <>
            
        </>
    )
}

export default UserCard